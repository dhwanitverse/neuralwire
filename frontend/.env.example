# Base URL the frontend uses for API calls.
# Leave as "/api" to use the built-in Next.js proxy to the backend.
NEXT_PUBLIC_API_URL=/api

# Absolute backend URL (used for non-proxied requests / OAuth redirects).
NEXT_PUBLIC_BACKEND_URL=http://localhost:5000
